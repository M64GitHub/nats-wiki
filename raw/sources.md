# Raw sources manifest

Immutable originals live in subfolders of `raw/`, one folder per collection. Never edit them
after creation; derived copies (UTF-8 conversions, PDF text extracts) go next to the original
with a suffix (`-utf8.txt`, `.txt`). One row per collection; the viewer shows this table on the
Sources page.

| collection | path | origin | fetched | notes |
|---|---|---|---|---|
| adr | `raw/adr/` | https://github.com/nats-io/nats-architecture-and-design (tarball of `main`) | 2026-08-31 | 54 ADRs plus the repo index (`_repo-index.md`) and the ADR template. Apache-2.0. Triaged into `inbox/adr-toc.md` by `tools/triage-adrs.py`. |
| nats-docs | `raw/nats-docs/` | https://docs.nats.io — paths taken from the site's `llms.txt`, fetched by `tools/fetch-docs.py` | 2026-08-31 | 861 pages of the docs in their Markdown form, mirroring the site tree (`concepts/`, `learn/`, `reference/`, `tutorials/`, `release-notes/`). `reference/config/` (621 pages) is generated from the server, not hand-written — `tools/build-config-table.py` turns it into `inbox/config-keys-table.md`. Docs are versioned; this is the 2.14 (latest) tree. Each file carries a one-line provenance header. Copyright Synadia Communications, Inc. |
| synadia-blog | `raw/synadia-blog/` | https://www.synadia.com/blog/ — individual posts, fetched with `curl` and converted by `tools/html-to-text.py` (the `.html` original is kept next to the `.txt` extract) | 2026-08-31 | Public Synadia engineering blog. Posts: `nats-jetstream-high-ram-usage` (Andrew Connolly, 2025-08-08, series *NATS Community FAQs*) and `jetstream-design-patterns-for-scale` (Andrew Connolly, 2026-06-06, titled *JetStream Anti-Patterns*, series *Design Patterns for Scaling NATS*). Site navigation and footer survive at the top and bottom of the extract — that is intended, the extract is verbatim. Copyright Synadia Communications, Inc.; summarize and paraphrase only. Added 2026-09-01 for step 2 of `inbox/plan-delivery-timing-2026-09-01.md`: `jetstream-reliable-delivery-dlq-replay` (Andrew Connolly, 2026-07-24, *Reliable Message Delivery in NATS JetStream: Acks, Retries, Dead Letters, and Replay*) — the dead-letter pattern built on the max-deliveries advisory, and the source of the nak/backoff contradiction this wiki settled on the binary. **Two of its claims are wrong at v2.14.6** and are recorded as `inbox/docs-issues.md` #39. Added 2026-09-01 for step 4: `delayed-message-scheduling-nats-jetstream` (Peter Humulock, 2026-04-09) — the readable account of the message scheduler, with the four `Nats-Schedule` formats against the version each arrived in, six gotchas, and the **target-must-differ-from-schedule-subject** rule that ADR-51 does not state (confirmed on the binary as `10190`). Added 2026-09-02 for step 4 of `inbox/plan-the-runnable-scouts-2026-09-02.md`: `how-many-subjects-jetstream-stream` (Andrew Connolly, 2026-05-20, *How Many Subjects Should a NATS JetStream Stream Have?*) — the only published per-subject memory figure ("roughly a few hundred bytes", ~10 M subjects ≈ 3–4 GB) and the design advice to size on consumption pattern rather than subject count. |
| synadia-insights | `raw/synadia-insights/` | https://www.synadia.com/insights/checks/ — the check-definition pages of Synadia's *Insights* monitoring product, fetched with `curl` and converted by `tools/html-to-text.py` (the `.html` original is kept next to the `.txt` extract) | 2026-09-02 | **Undated and unsigned** product documentation, cited as what a commercial monitor watches and never as a dated claim. `nats-subject-count-threshold` (check `JETSTREAM_025`: fires when a stream's subject count passes the operator-set metadata keys `io.nats.monitor.subjects-warn` / `-critical`; names memory, consumer filtering and **recovery time** as the three costs of cardinality) and `nats-stream-message-limit` (check `JETSTREAM_003`: fires at 90 % of `max_msgs`; the sizing formula `max_msgs = publish_rate × retention_window × safety_factor`). The metadata keys are a product convention, not a server feature. Added 2026-09-02 for step 4 of `inbox/plan-the-runnable-scouts-2026-09-02.md`. Copyright Synadia Communications, Inc.; summarize and paraphrase only. |
| release-notes | `raw/release-notes/` | https://github.com/nats-io/nats-server/releases via the GitHub API (`releases/tags/<tag>` and `releases?per_page=100`) | 2026-08-31 | `v2.14.0.md` is the release body verbatim. `_tags-and-dates.md` is the full tag list — **291 tags** with publish date and prerelease flag — so every version and date this wiki states is traceable to one file. Confirms there is **no 2.13**. Apache-2.0. Added 2026-09-02: `v2.14.1.md` … `v2.14.6.md`, the six patch-release bodies verbatim (`releases/tags/<tag>`, header with the publish date), fetched for the mirror and filestore lines of v2.14.1, v2.14.2 and v2.14.4 (step 2 of the runnable-scouts plan) and kept whole for phase D's per-minor ingest. Added 2026-09-03: `v2.10.16.md`, `v2.10.17.md`, `v2.11.2.md`, `v2.11.5.md` — the bodies of the releases whose notes fix a *redelivery of acknowledged messages* (#5419, #5482, #5533; the 2.11.2 consumer-consistency change and #6698) and the `DeliverLastPerSubject` ack defect of issue #6921 (#7005 in v2.11.5), written from the `releases?per_page=100` pages cached on 2026-09-02 (the same body the per-tag endpoint returns), for step 5 of the runnable-scouts plan. Added 2026-09-03 (phase D, step 1): the remaining **59 release bodies** of the 2.10–2.14 lines — every non-prerelease tag from `v2.10.0` to `v2.12.15` not yet here (27 of 2.10, 16 of 2.11, 15 of 2.12, including `v2.10.27-binary` and `v2.11.1-binary`, the CVE-2025-30215 binary-only releases) plus `v2.15.0-preview.1` by name — written by `tools/triage-releases.py --fetch` from `releases?per_page=100` (three pages, 291 releases; the live fetch matched the 2026-09-02 cache tag for tag, body for body, date for date). **70 bodies in all**, one file per release; the 11 written earlier were left as they were and still match the archive. The 129 RC and preview bodies are deliberately not written: each GA body is the consolidated changelog of its RCs, and `_tags-and-dates.md` records the RC tags and dates. `inbox/relnotes-toc.md` is generated from this folder. **2026-09-03 (phase F step 2)**: `v2.2.0.md` — the oldest body here, out of `triage-releases.py`'s 2.10+ range, kept for the arrival of headers, `HPUB`/`HMSG` and `no_responders` (which the body does not name; `raw/nats-server-src/headers-arrival-v2.2.0.md` holds the source at v2.1.9 and v2.2.0). |
| gh-discussions | `raw/gh-discussions/` | https://github.com/nats-io/nats-server/discussions — fetched one thread at a time through the GitHub **GraphQL** API (`repository.discussion(number:)`), which returns the original post, the chosen answer, every comment and every reply as Markdown | 2026-08-31 | One file per thread, `gh-<number>.md`, with a provenance header and the category, open date, author and whether an answer was chosen. Threads so far: `gh-3569`, `gh-4342`, `gh-4535`, `gh-5044`, `gh-5606`, `gh-6070`, `gh-6605`, `gh-7017`, `gh-7190`, `gh-7505`, `gh-7684`, `gh-7831`, `gh-7854`, `gh-7982` (all but `gh-6605`, `gh-7831` and `gh-7982` added 2026-08-31). `gh-7017` is stored despite having **no replies at all** — an unanswered public question is evidence about the documentation, and it is the question `inbox/question-bank.md` row 51 was mined from. GraphQL is used in preference to scraping the HTML: the bodies come back as the authors wrote them, so a claim can be quoted exactly. Public threads only. Added 2026-08-31 for plan step 5 (the second gotcha pass): `gh-5924.md` (filestore directories vanishing from a tmpfs `store_dir`), `gh-7463.md` (a corrupt Raft WAL on 2.9.8, fixed by upgrading), `gh-7834.md` (the same JetStream domain on both ends of a leafnode; unanswered), `gh-6490.md` (`has high message lag`), `gh-6746.md` (watching several KV keys on one watcher; self-answered), `gh-5243.md` (1000 KV watchers stalling a cluster; unanswered) and `gh-5859.md` (unexpected `nats: timeout`; unresolved). Added 2026-08-31 for plan step 6 (topology): `gh-6328.md` (JetStream on a single node behind gateways — answered by a maintainer with "you don't even need a super-cluster"), `gh-7438.md` (multi-region without sacrificing availability; a maintainer answers the architecture question and the two follow-ups about the *downsides* are **unanswered**), `gh-7881.md` (cross-domain JetStream sourcing; **no maintainer reply**, one community comment three months later), `gh-5941.md` (restricting what a leafnode exports and imports; answered by a maintainer, with an **unanswered follow-up showing the answer not working in config mode**), `gh-4823.md` (duplicate messages on a leafnode cluster attached to a supercluster; answered) and `gh-7494.md` (performance degradation in a global super-cluster; **unanswered**). Added 2026-08-31 for step 1 of `inbox/plan-the-unread-chapters-2026-08-31.md`, scouting question-bank row **Q65**: `gh-7749.md` (should JetStream use `hostPath` on Kubernetes — opened 2026-01-21, answered five months later by **@MILLERMARRU, a community member, not a maintainer**, with no chosen answer and no maintainer reply) and `k8s-760.md`, the **only file here from `nats-io/k8s` rather than `nats-io/nats-server`** (https://github.com/nats-io/k8s/discussions/760, sharing one PVC between JetStream replicas — answered by **@caleblloyd, a chart maintainer**, with the design rationale: JetStream needs fast **block** storage, not NFS or other file storage, and cloud block storage takes a single writer, which is why the chart gives each replica its own PVC). Added 2026-08-31 for step 4 of `inbox/plan-the-unread-chapters-2026-08-31.md`, question-bank row **Q75**: `gh-6836.md` (object-store `ls` slow, sometimes timing out, while an upload runs — opened 2025-04-25, **one comment, by the asker, and no reply from anyone else**; still open and unanswered sixteen months later, which is why the answer on [[object-store-list-is-slow]] is measured on the binary rather than cited). Added 2026-09-01 for step 6 (`learn/monitoring`), question-bank rows **Q60** and **Q61**: `gh-7362.md` (how are `routez`/`connz` `rtt` measured — **answered** by a maintainer as "periodic PING/PONG response times", but the period is never given and the reporter's follow-up, that `/connz` `rtt` never updates over minutes, gets a referral to commercial support instead of a reply) and `gh-7483.md` (is `/varz` `cpu` relative to a container's vCPU or the host's cores — **closed with zero comments and no answer**). Naming: `gh-<n>.md` is a `nats-io/nats-server` discussion; a thread from another repo carries that repo in the slug. Added 2026-09-01 for step 1 of `inbox/plan-delivery-timing-2026-09-01.md`, question-bank rows **Q16**, **Q17** and **Q19**: `gh-6628.md` (`ack_wait` vs the duplicate window — answered by a maintainer with "two different settings that are not related to each other", and the asker's real cause turns out to be a **pull batch of 100**), `gh-6350.md` (exponential backoff — a **marked** maintainer answer giving two mechanisms, consumer `Backoff` for an implicit failure and `nakWithDelay` for an explicit one) and `gh-4972.md` (does `NakWithDelay` hold a `max_ack_pending` slot — **working as designed** per @ripienaar, "max pending is used to manage ordering", with @derekcollison arguing in the same thread that it should change and **no fix version named**). Added the same day for step 2, question-bank row **Q18**: `gh-5631.md` (a nak that did not redeliver immediately on 2.10.14 with the C# client — **zero comments, still unanswered** two years later; stored because the wiki settles the question on the binary instead). Added 2026-09-01 for step 3, question-bank rows **Q29** and **Q30**: `gh-7672.md` (are cron schedules supported — **marked** answer: only single scheduled messages shipped in 2.12, cron is 2.14, and the asker's `message schedules pattern is invalid` is a version problem wearing a syntax problem's clothes) and `gh-7628.md` (the message scheduler vs `NakWithDelay` for 100K+ pending schedules — **marked** answer: "Nak is not meant for that purpose", and the scheduler scales because it is "built on top of the per-message TTL work"). Added 2026-09-01 for step 4, the dead-letter scope test (question-bank rows **Q106** and **Q107**): `gh-4994.md` (AckWait does not publish to a DLQ with zero clients — **marked** answer from @derekcollison, "We do not have automated DLQs… by design since we truly separate consumers from stream semantics", plus the confirmation that with nothing fetching a pull consumer's delivery count never advances) and `gh-7590.md` (Ideas, **still open**: the max-deliveries advisory carries no payload; @ripienaar explains the objection is sensitive payloads leaving NATS, and a later commenter disputes the premise — settled on the binary in favour of the commenter). `gh-7533.md` and `gh-6892.md` (added 2026-09-01) are the two threads of the meta-layer plan's step 2 — quorum loss after days of stable operation seen from MQTT, and evicting a sick-but-not-dead host; **both had zero comments and no answer** when fetched, and the files say so. Added 2026-09-02 for step 2 of `inbox/plan-the-runnable-scouts-2026-09-02.md`: `gh-8417` (KV mirror on file storage 65× slower than on memory — answered: an everything-matching `FilterSubject` on a mirror takes the per-subject path) and `gh-8444` (mirror catch-up 2.9× slower under a cold-scanning consumer — unanswered by maintainers as of 2026-09-02, one community comment). Rendered by a small script from the GraphQL JSON: the chosen answer is marked `**chosen answer**` on its comment, replies are `### Reply —` under their comment. Added 2026-09-02 for step 4 (stream scale, rows 4/5/9/13): `gh-8001` (JetStream startup ~6.5 min for 50 M messages on 2.12.5 — **no maintainer answer after the reporter's goroutine dump**, which this wiki read: the time is in `startingSequenceForSources`, a sourcing stream's backward scan), `gh-8333` (high-cardinality subjects — one maintainer comment, ~100 MB of RAM per million small subjects, not marked as the answer), `gh-5202` (max unique subjects — answered: the in-memory ART index since 2.10.9), `gh-7147` (a one-billion cap? — no answer; a maintainer's stream at 1,174,510,552 messages), `gh-7032` (largest known-good `MaxMsgs` — answered: no hard limit). Fetched through `tools/fetch-discussion.py` into `local/scratch/gh/` on 2026-09-02 and promoted with `--render-only --out`. Added 2026-09-03 (phase D, step 3): `gh-6005` (sourcing from a memory stream stalls after a restart — the 2.10.19 clipping change and its 2.10.22 revert) and `gh-6748` (CVE-2025-30215: who builds the Docker image, and when it follows a binary-only release). Added 2026-09-03 (phase E, step 1): `gh-5768` (reconciling connected clients after a missed `CONNECT` event — `$SYS.REQ.SERVER.<id>.CONNZ`, paged) and `gh-5902` (`LEAFNODE.CONNECT` events that never arrive on a single hub — settled from the source: the sender is a no-op without gateways). Added 2026-09-03 (phase E, step 2): `gh-3944` (which subjects a stream actually holds — `STREAM.INFO` with `subjects_filter`). Added 2026-09-03 for phase E step 3: `gh-2818` (are the byte and message counters exact — "Yes"), `gh-3857` (consumer pending metrics in surveyor or the exporter, unanswered), `gh-6182` (what to alert on with the exporter, no replies), `gh-5128` (HA assets: "2k HA Assets" per server in Synadia's global clusters). Added 2026-09-03 (phase F step 1): `gh-7577.md` — *Message ordering guarantees in core NATS* (2025-11-24, answered by a maintainer the same day; the thread behind question-bank row 25), fetched whole with `tools/fetch-discussion.py 7577 --out raw/gh-discussions`. **2026-09-03 (phase F step 2)**: `gh-2760.md` (one connection or two, the chosen answer on head-of-line blocking — bank row 138). |
| jsm-go | `raw/jsm-go/` | https://github.com/nats-io/jsm.go at tag **v0.4.1**, `schemas/jetstream/api/v1/stream_configuration.json` and `consumer_configuration.json` from `raw.githubusercontent.com` | 2026-09-03 | The two JetStream configuration JSON schemas the docs' reference pages are generated from, verbatim, named with the tag (`_provenance.md` says which). The consumer schema is the only public place the consumer fields' descriptions and defaults can be read, because the docs page collapses the object (docs issue #4). Apache-2.0. |
| nats-cli | `raw/nats-cli/` | the `nats` CLI **0.4.0** binary on this machine, `nats stream add --help`, `stream edit --help`, `consumer add --help`, `consumer edit --help` | 2026-09-03 | `help-0.4.0.md`: the four help texts verbatim — the flag names and defaults behind the CLI column of `wiki/reference/stream-and-consumer-config.md`. Captured output, not a fetched document; regenerate by re-running the four commands at a new CLI version. Added 2026-09-03 (the `share` query): `help-auth-account-exports-imports-0.4.0.md` — `nats auth account exports add --help` and `imports add --help` verbatim, the `--share`, `--token-position` and `--traceable` flags, and no `--private`. Added 2026-09-03 (phase F step 1): `help-core-0.4.0.md` — `nats request`, `reply`, `trace`, `server mappings`, `subscribe` and `publish` `--help` on 0.4.0, verbatim, for the core-NATS pages (`nats reply --queue` defaults to `NATS-RPLY-22`; `nats request --replies=1 --reply-timeout=300ms`; `nats trace --deliver`). **2026-09-03 (phase F step 2)**: `request-reply-0.4.0.md` — `cli/req_command.go` and `cli/reply_command.go` at v0.4.0 from the Go module cache, the gather loop, the exit paths and the reply handler, with line numbers. **2026-09-04 (phase F step 3)**: `reconnect-0.4.0.md` — `cli/util.go`'s `natsOpts()`, `internal/util/backoff.go`, `cli/reply_command.go` and `cli/rtt_command.go` at v0.4.0 from the Go module cache, with line numbers: `MaxReconnects(-1)`, `IgnoreAuthErrorAbort()`, the 44-step 500 ms → 20 s backoff table and its `jitter()`, the six `>>>` lines and which are trace-gated, the twice-registered error handler, and `nats reply`'s `Drain()` + `log.Fatalf`. |
| nats-server-src | `raw/nats-server-src/` | https://github.com/nats-io/nats-server at tag **v2.14.6**, individual `server/*.go` files from `raw.githubusercontent.com` | 2026-08-31 | `constants-v2.14.6.md` holds the exact line ranges this wiki quotes defaults from — `const.go`, `consumer.go`, `stream.go`, `filestore.go`, `server.go` — verbatim, with their real line numbers, so each value links to `…/blob/v2.14.6/server/<file>#L<line>`. Only the quoted ranges are stored, not whole files. `README-v2.14.6.md` is the repository README at the same tag, added 2026-08-31 — the only public source read for CNCF membership, the licence statement and the Trail of Bits / OSTIF security audit. `route-v2.14.6.md` (added 2026-08-31) holds the `server/route.go` ranges for cluster formation — the cluster-name check on both sides of a route, the dynamic-name adoption branch, and the route listener's startup log lines. `systemd-units-v2.14.6.md` (added 2026-08-31) is `util/nats-server.service` and `util/nats-server-hardened.service` verbatim — the units `learn/deployment/hardening.md` adapts. `const-lame-duck-v2.14.6.md` (added 2026-08-31) is the `server/const.go` range that `constants-v2.14.6.md` stops short of, holding `DEFAULT_LAME_DUCK_DURATION` and `DEFAULT_LAME_DUCK_GRACE_PERIOD`. `signal-v2.14.6.md` (added 2026-08-31) is `server/signal.go`'s `handleSignal` and `CommandToSignal` plus the `Command` constants — what each signal does, including SIGTERM during a drain, and which signal each `--signal <command>` name sends. `lame-duck-v2.14.6.md` (added 2026-08-31) is `Server.lameDuckMode()` in full with `validateOptions`'s grace-period rule and `raft.go`'s `transferRaftLeaders` — the ordering and arithmetic behind `lame_duck_duration`. `snapshot-restore-v2.14.6.md` (added 2026-08-31) is the snapshot/restore path across `jetstream_api.go`, `stream.go` and `memstore.go` — the chunk and window clamps, the two restore checks, and the `no impl` a memory stream returns. `auth-tls-v2.14.6.md` (added 2026-08-31) spans eight files — `const.go`, `opts.go`, `auth.go`, `auth_callout.go`, `monitor.go`, `server.go`, `events.go`, `stream.go` — and holds the ranges behind docs issues #19–#21: the real TLS and auth timeout defaults and `getDefaultAuthTimeout`, `Varz.TLSCertNotAfter` and the function that fills it, the certificate-to-user mapping order, `fillConnectOpts` (which is why auth callout verifies nothing the client sent), the fabricated `$G` no-auth user, the `$SYS.REQ.CLAIMS.*` subjects and `ExternalStream`. Apache-2.0. `jetstream-resources-v2.14.6.md` (added 2026-08-31) spans `jetstream.go`, `disk_avail.go`, `jetstream_api.go`, `jetstream_cluster.go`, `stream.go`, `filestore.go`, `opts.go` and `raft.go` — `dynJetStreamConfig` and `diskAvailable` (the real storage default), `finalizeDynamicMaxStore` (new in 2.14.6), `reserveStreamResources` / `checkBytesLimits` / `accountReservation` (what 10047 and 10028 actually compare), `handleOutOfSpace` and its two callers, plus the definition sites behind docs issue #22. `leafnode-js-domains-v2.14.6.md` (added 2026-08-31) is `Server.addLeafNodeConnection`'s JetStream block with `generateJSMappingTable` and the deny lists — the three outcomes of a leafnode carrying JetStream, and the server's explicit guard against two identical domain names. `jetstream-log-warnings-v2.14.6.md` (added 2026-08-31) holds every JetStream warning format string this wiki quotes, with `streamLagWarnThreshold`, `resetClusteredState`, the critical-write-error handlers, and the JetStream API queue drain that answers a request with nothing at all. `topology-v2.14.6.md` (added 2026-08-31) spans `server.go`, `errors.go`, `leafnode.go`, `gateway.go`, `client.go`, `monitor.go`, `opts.go` and `const.go` — `validateOptions` and the leafnode/gateway system-account requirement, `validateGatewayOptions` (there is **no** default gateway port), `validateCluster`'s adoption of `gateway.name` as the cluster name, the two halves of the gateway name-mismatch log pair, `sendMsgToGateways` (geo-affinity as an exclusion list), `pmrCollectQueueNames`, the fast-producer stall and its `stalled_clients` / `stalls` counters, `parseLeafUsers` (which accepts no `permissions`), and the single place `DEFAULT_LEAFNODE_PORT` is used. It also records **five behaviours reproduced locally**, with the exact configs and output — including the docs' own composed topology config failing to start, and `nats-server -t` passing it; those first runs were made on the **v2.14.5** binary, which was what the machine had. `topology-observed-v2.14.6.md` (added 2026-08-31, after `brew upgrade nats-server`) re-runs all five on **v2.14.6**, so the observations and the source ranges name the same release: **all five reproduce identically**, down to the configuration checksums `-t` prints. Both files are kept — the first is what was actually run, the second is the confirmation. `defaults-observed-v2.14.6.md` (added 2026-08-31) is the evidence behind the config-default sweep (`tools/check-defaults.py`, report `inbox/check-defaults-v2.14.6.md`): the `setBaselineOptions` compression ranges in `opts.go`, the `mqttDefault*` constants and their use sites in `mqtt.go`, `validateMQTTOptions`, and **four runs of the v2.14.6 binary** — a hub/leaf pair with no compression configured (`/leafz` reports `s2_uncompressed`) against the same pair carrying the documented `accept` on both ends (`off`), and an MQTT server with and without a port (no listener and no log line without one). Behind docs issues #27–#29. `compression-purge-discovery-observed-v2.14.6.md` (added 2026-08-31) is the evidence behind docs issue #30 and behind [[stream-compression]], [[maximum-messages-exceeded]] and [[how-clients-reach-a-cluster]]: three experiments run on the **v2.14.6** binary with **nats CLI 0.4.0** — a live `compression` change proved not to reach the running store until it is re-created (block magic bytes and sizes before and after a restart), a full `DiscardNew` stream refusing publishes with `10077` and **nothing in the server log**, the three `nats stream purge` options and the `10003` the server returns for `seq`+`keep` together, and the `INFO` `connect_urls` a client is actually given — standalone, 2-node, 3-node, with `cluster { no_advertise }` and with `client_advertise`. The machine's LAN address is written as `<host-ip>`; nothing else is edited. `filestore-v2.14.6.md` (added 2026-08-31) is the filestore's on-disk layout across `filestore.go`, `stream.go` and `memstore.go` — the `bytes`/`rbytes` split, the filestore constants (block sizes, `compactMinimum`, `FileStoreMinBlkSize`, `highCardinalityThreshold`), `emptyRecordLen` (30), `dynBlkSize` and `autoTuneFileStorageBlockSize` (the two places a block size is chosen), `shouldCompactInline`/`shouldCompactSync` **and the two call sites that skip the last block**, the record-format comment and the code that writes it, `updateAccounting`, `checkLastBlock`, `fileStoreMsgSize`, the `index.db` writer with its 2-minute timer and its size estimate, and `memStoreMsgSizeRaw` — the reason a memory stream reports different bytes for the same message. `tls-reload-observed-v2.14.6.md` (added 2026-08-31) is the evidence behind question-bank row **Q97** and docs issue #34: **eight experiments and two controls run on the v2.14.6 binary with nats CLI 0.4.0** — a client listener's certificate replaced in place and picked up by `nats-server --signal reload` (`/varz` `tls_cert_not_after` moving while `config_digest` and every log line stay identical to a no-op reload's), the same with a fresh keypair, the client's own view through `nats account tls`, a mismatched certificate/key pair and a missing certificate file (both logged as `[ERR] Failed to reload server configuration` while the **signal command still exits 0** and the old certificate stays live), and a hub/leaf pair with `verify_and_map` proving that a **leafnode remote's** `cert_file`, `key_file` and `ca_file` all reload at v2.14.6 — in both shapes, new bytes behind the same path and a new path in the config — against the generated reference's *"On 2.11/2.12 the reload succeeds but the old certificate keeps being used"*. It also records that `verify_and_map` matches the certificate's RFC 2253 **DN string** rather than the bare CN, the failed first test design (Go filters client certificates against the server's acceptable-CA list, so an unknown-CA certificate is never offered), and the `nats server check` subcommand list at CLI v0.4.0, which contains **no certificate-expiry check**. The machine's LAN address is written as `<host-ip>`. `filestore-observed-v2.14.6.md` (added 2026-08-31) is the behavioural half, **eleven experiments run on the v2.14.6 binary with nats CLI 0.4.0**: the record length measured three ways and decoded byte-for-byte out of a `.blk` file, a block file proved equal to the reported bytes, a delete shown to make the file *bigger*, a purge shown to free it, the block size the server picks for seven stream shapes, `max_bytes` compared against both reported and real bytes, the last block proved never to compact across a sync interval and a restart, `index.db` measured at ~4 bytes per subject beyond the subject itself, recovery timed with and without `index.db` (**no measurable difference at this size — recorded so nobody derives one**), `/jsz` and `/varz` shown to report logical bytes, consumer `o.dat` measured against `max_ack_pending`, and a second server with `max_file_store: 4MB` holding 3.79 MB on disk while reporting 3% used — the evidence behind docs issue #33. Store paths are written as `<store>`; nothing else is edited. `object-store-observed-v2.14.6.md` (added 2026-08-31) is the object-store chapter run rather than read, **nine experiments on the v2.14.6 binary with nats CLI 0.4.0**: the `OBJ_<bucket>` stream config printed in full and matched field-for-field against the docs' own JSON, the 128 KiB default chunk size derived from an exact chunk count at three chunk sizes, a chunk size above `max_payload` shown to fail **client-side** leaving the previous object and zero orphan chunks, the raw metadata message with its `Nats-Rollup: sub` header and its **zero `mtime`** (the observable behind ADR-20's "modified time is never stored"), the soft-delete tombstone verbatim, disk reclamation timed at 0/2/10/30/60 s after a 200 MiB delete (**98.4 % synchronous**, the residue bounded by one block), the four `$JS.API` calls `nats object ls` makes — including the ephemeral `last_per_subject` consumer created and destroyed per call — and the **Q75 measurement**: list latency idle vs under a sustained upload, on the written bucket and on an unrelated one, against core-NATS RTT and `STREAM.INFO` as controls. Also bucket-name charset, empty-bucket and missing-object exit codes, and `10109` on a sealed bucket. `mqtt-websocket-observed-v2.14.6.md` (added 2026-09-01) is `learn/mqtt` and `learn/websocket` run rather than read, **fifteen experiments on the v2.14.6 binary with nats CLI 0.4.0**: the **five `$MQTT_*` streams** the server creates for itself — which no public source names — with their retention and discard policies, created lazily on the first MQTT connection; what QoS 0/1/2 and a retained message each cost in stored bytes (question-bank **Q80**); the dedup record an abandoned QoS 2 handshake leaks into `$MQTT_qos2in` forever; `$MQTT_out` traced packet-by-packet to the outbound PUBREL handshake; **gh#7397's symptom reproduced** — stale sessions pinning QoS 1 messages through interest retention, and the clean-session reconnect that releases them; all ten topic→subject conversion rules; the six refused characters with the publish-closes/subscribe-`0x80` asymmetry; the **63/31** per-session subscription ceiling; MQTT v5 refused with CONNACK code 1; four startup errors verbatim; both silent no-listener cases; the WebSocket origin table **including the no-`Origin` row that is admitted**; and MQTT `stream_replicas` shown to be derived from the server's own `routes` list (R=2 on a real three-node cluster), with the `10005` an unsatisfiable replica count produces. It also records a **harness bug that produced a plausible false finding** and how it was caught. The instrument is `mqtt-probe-client-v3.1.1.py` in the same directory (added 2026-09-01): a ~110-line stdlib-only MQTT **3.1.1** client written for these runs — no MQTT client was installed on the machine — speaking the full QoS 2 handshake and exposing SUBACK return codes. `monitoring-observed-v2.14.6.md` (added 2026-09-01) settles question-bank rows **Q60** and **Q61**, neither of which `learn/monitoring` states: the quoted ranges of `monitor.go` (`Varz.CPU`/`Cores`/`MaxProcs` and their json tags), `server/pse/pse_linux.go` (the once-a-second `/proc/<pid>/stat` sampler, the **hardcoded `ticks = 100`** and its CGO comment, and the host-uptime term), `client.go` (`computeRTT` floored at 1ns, the connect-time initial `rtt`, `sendPing`/`processPong`, the MQTT exclusion, and the `needRTT` test) and `const.go`'s **`DEFAULT_RTT_MEASUREMENT_INTERVAL = time.Hour`** — plus every `JSAdvisory*Pre` constant at the tag and **two advisories produced on the binary and read off the wire**: `$JS.EVENT.ADVISORY.CONSUMER.MSG_NAKED.…`, which upgrades docs issue **#1** from a source constant to an observed subject, and `$JS.EVENT.ADVISORY.CONSUMER.MAX_DELIVERIES.…` with its full body, behind docs issue **#36**. `priority-groups-observed-v2.14.6.md` (added 2026-09-01) settles docs issue **#37**: ADR-42's rules that a consumer's priority policy and groups cannot be changed after creation and that more than one group is an error, tested against the v2.14.6 binary with nats CLI 0.4.0 — all three forbidden transitions accepted and two groups accepted and stored, with the two ADR rules that *do* hold (`10162` on a 17-character group name, `10178` on a push consumer) run as controls, and the 2-minute `priority_timeout` the server fills in for `pinned_client`. The transcript records what was **not** tested: whether a two-group consumer serves both groups, behaviour with active pinned clients, and the clustered case. `nak-backoff-observed-v2.14.6.md` (added 2026-09-01) settles question-bank rows **Q17**, **Q18** and **Q19** and produced docs issues **#38** and **#39** and server issue **SI-2**: thirteen experiments on the v2.14.6 binary, plus the three ranges of `server/consumer.go` at the tag that explain them — `config.AckWait = config.BackOff[0]` (`:658`), the delayed nak's `p.Timestamp = time.Now().Add(-o.cfg.AckWait).Add(d)` (`:3231`) and `checkPending`'s `deadline = int64(o.cfg.BackOff[dc])` (`:6066`). A **bare** `-NAK` is immediate with or without a backoff; a nak carrying a delay waits `delay + (BackOff[dc] − BackOff[0])` — 2s asked, 12s given — and `-NAK {}` is **not** a bare nak. With controls: no-answer redelivery on the documented schedule (5.00 · 10.00 · 15.00) and delayed naks on a backoff-free consumer (1.95 · 1.95 · 1.95). Also the blog's own consumer config sent to `$JS.API.CONSUMER.CREATE` (`ack_wait: 30s` stored as **1s**) and the `max_ack_pending` re-run behind Q19 (cap 2, both nak'd with a 30s delay, next pull `408`). The transcript records a **harness bug that would have produced a false reading** — consumers created with no filter subject on a `test.>` stream, so every table since carries the stream sequence beside the delivery count. The instrument is `nats-probe-client.py` in the same directory (added 2026-09-01): a ~90-line stdlib-only NATS client, written because **nats CLI 0.4.0 cannot send a delayed nak at all**. `message-schedules-observed-v2.14.6.md` (added 2026-09-01) is ADR-51 run rather than read, because **no `learn/` chapter of the docs mentions message scheduling at all**: **26 publishes and 4 stream operations** on the v2.14.6 binary with nats CLI 0.4.0, pinning **ten** error codes to their exact conditions (10186, 10187, 10188, 10189, 10190, 10191, 10192, 10203, 10212, 10223 — plus the two stream-level refusals that arrive as the generic **10052**), the generated message's real headers (`Nats-Scheduler` is a **subject**, `Nats-Schedule-TTL` becomes `Nats-TTL` on the **generated** message — both contradicting `reference/jetstream/api/headers.md`), the stored side effects of `allow_msg_schedules` (`allow_rollup_hdrs: true`, `deny_purge: false`, API level 2), a past-dated `@at` firing immediately, and the five `Nats-Scheduler` conditions behind 10212 quoted from `server/stream.go` at the tag. It also records that **`nats pub` without `-J` is a core publish and silently hides a rejected JetStream publish** — the trap that made the first run report every invalid publish as accepted — and that **nats CLI 0.4.0's `--schedule-after` emits a schedule the server always rejects** (docs issues #40, #41, #42). `jetstream-cluster-v2.14.6.md` (added 2026-09-01) holds **65 ranges** across `jetstream_cluster.go`, `raft.go`, `jetstream_api.go`, `monitor.go`, `jetstream.go`, `server.go` and `opts.go` — the meta group's name, store and structs, `setupMetaGroup` (bootstrap, recovery, observer mode), `isLeaderless`, `checkForOrphans` and its 30-second timer, the meta apply loop's snapshot criteria and the `meta_compact*` keys, `processAddPeer` / `processRemovePeer`, `applyMetaEntries`, `processLeaderChange`'s log lines, the meta leader's five system-account subscriptions (including the real `$JS.API.SERVER.REMOVE`), the API gate that answers `10008` or nothing, the `raft.go` timing constants (none configurable), `bootstrapRaftNode`'s expected-size arithmetic, `StepDown`, `Reset`, `Quorum`/`lostQuorum`, `trackPeer`'s five-minute rejoin, `MetaClusterInfo`, the five `/healthz` meta messages, and the cluster error codes from `errors.json`. `jetstream-cluster-observed-v2.14.6.md` (added 2026-09-01) is the run: **a three-node cluster on the v2.14.6 binary with nats CLI 0.4.0** — bootstrap (0.28 s) vs recovery (5.3 s) vs stepdown (0.5 s) vs a SIGKILLed leader (3.5 s), the on-disk `_meta_` layout, `/jsz`'s `meta_cluster` on a follower and on the leader (and that `?meta=1` is ignored), the `RAFT Meta Group Information` table, two of three servers killed (the survivor claims leadership for 10 s; `10008` only after), the fate of timed-out creates and publishes once quorum returned (committed if the same server re-won, discarded otherwise), a standalone server's stream deleted **30.0 s** after joining, a live peer-remove with the `SERVER.REMOVED` advisory body, the removed server re-added **five minutes** later, and the documented `$JS.API.META.SERVER.REMOVE` answering `10039`. Behind docs issues #43–#46. It records that `nats pub -J` prints `Published` before waiting for the ack, which invalidated one column of the first run. `kick-ldm-and-mqtt-session-v2.14.6.md` (added 2026-09-01) holds nine ranges of `mqtt.go`, `events.go`, `server.go` and `jetstream_cluster.go` — `mqttSession.save` and its `Nats-Expected-Last-Subject-Sequence` header (the mechanism behind `wrong last sequence: 0 (10071)`), the `$SYS.REQ.SERVER.<id>.KICK` / `.LDM` subjects and handlers, `DisconnectClientByID` / `LDMClientByID`, and the stream-leader log lines. `kick-ldm-observed-v2.14.6.md` (added 2026-09-01) runs both requests against a live `nats sub` on the cluster above: KICK disconnects (`EOF, will attempt reconnect`, back on another server in 2.5 s), LDM only sends an INFO and nats CLI 0.4.0 stays connected, an unknown cid answers `no such client or leafnode id`, and the full list of per-server request subjects at the tag — none by IP, none bulk, none for routes. `raftz-v2.14.6.md` (added 2026-09-01) is `/raftz` read and run: `RaftzOptions`, `RaftzGroup`, `RaftzGroupPeer`, `HandleRaftz` (the HTTP names are `acc` and `group`) and `Raftz` (the account filter defaults to the system account), the parameter-decode lines of `HandleAccountz`, `HandleJsz`, `HandleLeafz`, `HandleSubsz`, `HandleGatewayz` and `HandleConnz`, `StreamState` (the `wal` object) and the 256 KB / 512-entry append batch — then the endpoint on the four-node cluster from a follower and from the leader, six filter combinations, and the documented `account=` parameter shown ignored on `/raftz` and `/accountz`. Behind docs issues #47 and #48. `jetstream-cluster-lab-rerun-observed-v2.14.6.md` (added 2026-09-02) repeats §1, §2 and §6 of the meta-layer run with the cluster started by `bash tools/lab/cluster.sh up 3` — the same numbers, plus a probe with `monitor.go:3584–3589` showing that `Healthcheck failed` is logged once per `/healthz` request, not once a second. Added 2026-09-02: `mirror-v2.14.6.md` (`stream.go` — the inbound queue limits, the internal consumer names and their deletion, `processMirrorMsgs`, `processInboundMirrorMsg`, `skipMsgs`, the consumer-create request and its fallback name, plus the `mirror-%s` naming at v2.10.0 and v2.12.0 for comparison; `filestore.go` — `firstMatching`'s linear-scan heuristic, `LoadNextMsg`, `StoreMsg`, `SkipMsgs`), `mirrors-observed-v2.14.6.md` (three runs: a KV bucket mirrored on file and on memory read four ways; the mirror's catch-up alone and under three Go readers; an object bucket mirrored across two domains, with and without the transform) and `mirrorlab.py`, the stdlib-only python client those runs use (fill with an ack window, pull-consume, poll `STREAM.INFO` until lag 0). Added 2026-09-02 for step 4: `filestore-recovery-v2.14.6.md` (`jetstream.go` — the `Took … to start JetStream` and `Restored … in` timers and the parallel task queue; `stream.go` — `recoverStream` → `setLeader` → `subscribeToStream` → `setupSourceConsumers`, inline on R1, and `startingSequenceForSources`, the backward scan; `filestore.go` — `psi`/`psim`, `highCardinalityThreshold`, the recover decision in `newFileStoreWithCreated`, `recoverFullState` whole, `recoverMsgs`, `recoverMsgBlock`, `rebuildState`, `flushStreamStateLoop`, `_writeFullState`, `stop`, `LoadPrevMsgMulti`, `prevMatchingMulti`, `checkSkipFirstBlock*`; plus the bodies of PRs #8282 and #8516 and the release lines that changed recovery, from the GitHub API) and `stream-scale-observed-v2.14.6.md` (runs D, E and F: a 50 M-message stream restarted four ways, a sourcing stream with an empty source, a 1.2 M-subject stream against a 6-subject one, `--max-msgs` past a billion). Added 2026-09-03: `redelivery-observed-v2.14.6.md` with `redelivery-runG.sh`, `redelivery-runI.sh`, `redelivery-runH.sh`, `redelivery-runH-batch.py`, `redelivery-runH-repeat.sh` (runs G, H, I: issue #6921's recipe not reproducing on 2.14.6; a redelivery loop as the CLI, `consumer info`, the JSON counters and the `-DV` trace show it, with nothing at the default log level; `backoff: [10000]` stored as a 10 µs `ack_wait` and what that does to an acking client — a race the ack usually wins on localhost, and every message delivered exactly `max_deliver` times once the handler takes 5 ms). Added 2026-09-03: `stree-arrival-v2.10.10.md` — `server/filestore.go` lines 172–184 at tags v2.10.9 and v2.10.10 and the `server/stree/` directory listing at four tags, read through the GitHub contents and commits API, settling that the per-subject subject tree shipped in v2.10.10 (a maintainer had said 2.10.9). `feature-flags-dial-timeout-and-2.15-subjects.md` (added 2026-09-03) quotes, with line numbers, the leafnode `dial_timeout` parsing and its `DEFAULT_ROUTE_DIAL` fallback (`opts.go`, `const.go`, `leafnode.go` at v2.14.6), the two feature flags of `feature_flags.go` at v2.14.6 and the three at **v2.15.0-preview.1** (the only file in this collection read at a tag other than v2.14.6, and it says so on every excerpt), the `$JS.API.CONSUMER.RESET` template, the preview's `EVACUATE` / `CANCEL_MOVE` / `META.RESCUE` subjects from `jetstream_api.go`, and the `AckFlowControl` constraints of `consumer.go`; behind docs issues #61–#63 and the 2.14 / 2.15 release-note summaries. `backported-keys-v2.11.17.md` (added 2026-09-03) quotes the `opts.go` parse sites at **v2.11.17** for the block-level `write_deadline`, `write_timeout` and `websocket { ping_interval }` keys the docs date to 2.12, and their absence at v2.10.29 — read from the release tarballs `tools/check-defaults.py` caches; behind docs issue #64 and the default diff of phase D step 8. `system-subjects-v2.14.6.md` (added 2026-09-03, phase E step 1) quotes `events.go` (the `$SYS` subject constants and the three intervals, the `PING.<Z>` / `<id>.<Z>` and per-account request tables, the event and request structs, the response envelopes, the connect/disconnect/auth-error/lame-duck/shutdown senders, the built-in account imports), `server.go` (the HTTP paths and every `HandleFunc`), `accounts.go` (`ServiceLatency` and `sendLatencyResult`) and **`jetstream_events.go` whole** — the authority the 24 generated advisory pages were swept against. `system-subjects-observed-v2.14.6.md` and `system-subjects-run.sh` (added 2026-09-03) are the runs: which paths the HTTP mux serves (`/statsz`, `/idz`, `/profilez` 404; `/stacksz`, `/debug/vars` 200), the same names answering over `$SYS.REQ.SERVER.PING`, the account events on a standalone server with two accounts (`CONNS` on both subjects, on every change and every 30 s; a leaf as a `CONNECT` with `kind: Leafnode`; no `LEAFNODE.CONNECT` without gateways), a failed auth, service latency on the export's own subject, `STATSZ` every 10 s, reload by message, lame duck and shutdown, and the account-scoped request forms as `sys` and as an ordinary user; behind docs issues #65–#72. `stream-consumer-config-v2.14.6.md` (added 2026-09-03, phase E step 2) quotes `StreamConfig` and `ConsumerConfig` with their nested structs, `checkStreamCfg` (the defaults and validations), `configUpdateCheck` (what a stream update may change), the batch constants, `setConsumerConfigDefaults`, `checkConsumerCfg`, `checkNewConsumerConfig`, `updateConfig`, the policy enumerations, the size constants, `JetStreamAccountLimits`, `JSLimitOpts`, the info and get-next request structs, and the two places a message timestamp is taken. `config-mutability-observed-v2.14.6.md` with `config-mutability-run.sh`, `-run2.sh`, `-run3.sh` (added 2026-09-03) is the run: every fixed, one-way and free field of a stream and a pull consumer tried by raw API update on 2.14.6 with the refusal strings, what sealing forces, `subjects_filter` on `STREAM.INFO`, an ephemeral's defaults, and a pull of `batch: 300` served in full against the docs' "Maximum: 256". `traffic-counters-and-ha-assets-v2.14.6.md` (added 2026-09-03, phase E step 3) holds the `client.go`, `monitor.go`, `consumer.go`, `jetstream.go`, `jetstream_cluster.go`, `raft.go` and `opts.go` ranges behind `wiki/reference/metrics.md`: the atomic traffic counters `/varz` and `/connz` report, the consumer-info branch that computes `num_pending` on the leader only, `ha_assets` as the Raft-node count and the `max_ha_assets` limit at group creation and placement. Added 2026-09-03 (the `share` query): `service-imports-v2.14.6.md` — the `server/accounts.go`, `client.go`, `events.go`, `jetstream.go`, `jetstream_api.go`, `stream.go` and `opts.go` ranges for the `Nats-Request-Info` header, the import's `share` flag, the three export guards (`checkAuth`, `checkActivation`) and the config parsers' import/export keys, plus the v2.10.0 ranges that show the header and `share` present there; `share-import-observed-v2.14.6.md` — four scenes on the v2.14.6 binary (without/with `share`, a two-hop chain, `max_payload: 256`, `share` on a stream import), with `share-import-run.sh` and `share-import-rawsub.py` beside it. Added 2026-09-03 (phase F step 1): `core-delivery-v2.14.6.md` — the control line, payload, pending and ping constants, `ClientOpts`/`defaultOpts`, the two `PUB` parsers, the `no_responders`-needs-headers check, the subscribe path with `max_subscription_tokens`, the echo skip, the account-mapping call and the 503 `HMSG` (`client.go`, `parser.go`), `max_subscription_tokens`/`no_header_support`/top-level `mappings` (`opts.go`), `AddWeightedMappings` and `selectMappedSubject` (`accounts.go`), `isValidSubject` (`sublist.go`), the `INFO` `headers` field and the lame-duck notice (`server.go`), the error values (`errors.go`), `/subsz` (`monitor.go`), the reload rule (`reload.go`); `core-delivery-observed-v2.14.6.md` with `core-delivery-run.sh` and `core-delivery-raw.py` beside it — seven runs on the binary and one on the lab (a whitespace subject misrouted, `max_payload` counted over header plus body, pedantic mode delivering after its `-ERR`, `max_subscription_tokens` not reloadable, `/subsz?test=`, `nats trace`, weighted and partitioned `mappings` counted over 200 publishes and reloaded, a restart and a lame duck as `nats sub --trace` prints them). Captured output, not a fetched document — regenerate with the script. **2026-09-03 (phase F step 2)**: `request-reply-v2.14.6.md` (`processMsgResults`'s queue selection, the 503 send, `subForReply`, the sublist's weight expansion, the routed queue weight, the gateway exclusion), `headers-arrival-v2.2.0.md` (`server.go` and `client.go` at v2.1.9 and v2.2.0 — headers and `no_responders` arrived in 2.2.0), `request-reply-observed-v2.14.6.md` (runs A–H in four passes on 2.14.6, the lab and a standalone leaf; scripts `request-reply-run.sh`, `-run2.sh`, `-run3.sh`, `-run4.sh` and `request-reply-subsz.py` beside it). **2026-09-04 (phase F step 3)**: `client-lifecycle-observed-v2.14.6.md` — the client's side of a fault, five runs on the lab and on standalone servers (a node stopped, a node in lame duck with the `ldm` INFO, `nats reply` drained under load, the stale link from both ends timed over six minutes, a pull consumer whose leader moves), with `client-lifecycle-run.sh`, `-run2.sh`, `-run3.sh`, `-stale-run.sh`, `client-lifecycle-raw-watch.py` and `client-lifecycle-stale-client.py` beside it. **2026-09-04 (phase F step 4)**: `client-errors-v2.14.6.md` — the generated table of all 58 client-protocol `-ERR` call sites at v2.14.6 plus 15 verbatim ranges (`ClosedState`'s 37 values, `errProto`, the `sendErr` family and the four credential paths, both server-side slow-consumer branches and `markConnAsClosed`'s skip-flush rule, `setExpiration` and its zero-length timer, the account's `expiredTimeout`, `handshake_first`'s parser and its fallback, and the startup warning's gate); and `client-faults-observed-v2.14.6.md` — runs A–C on 2.14.6 (a client-side slow consumer under `SetPendingLimits`, both server-side branches, a user and an account JWT expiring under a live connection on the wire and through three clients, and the four settings of `handshake_first` timed), with `client-faults-runA.sh`, `-runA2.sh`, `-runB.sh`, `-runB2.sh`, `-runC.sh`, `client-faults-deadsub.py`, `client-faults-slowsub.go`, `client-faults-mintjwt.go`, `client-faults-rawcreds.go` and `client-faults-authclient.go` beside it. |
| stackoverflow | `raw/stackoverflow/` | https://stackoverflow.com — one question per file through the Stack Exchange API 2.3 (`/questions/<id>` and `/questions/<id>/answers` with `filter=withbody`), the HTML bodies converted to Markdown by a short script with code blocks kept verbatim | 2026-09-03 | `so-<id>.md`, a provenance header, the question with its score, views, tags and dates, then every answer marked accepted or not. Content is CC BY-SA 4.0, attributed to its authors by display name. Added 2026-09-03: `so-78603662.md` — question-bank row 14's own thread ("messages being processed multiple times by my consumer even when messages are acknowledged", .NET, 2024-06-10, one answer, none accepted): a `Backoff = [10000]` with `MaxDeliver = 2`, which is a ten-microsecond ack deadline on the wire; the wiki's row 14 was mined from its title on 2026-08-31 and the body fetched here for the gotcha `consumer-keeps-redelivering`. |
| github-repos | `raw/github-repos/` | https://api.github.com/repos/{owner}/{name} and `.../releases/latest` and `.../readme`, through the `gh` CLI — fetched by `tools/fetch-repo-facts.py` | 2026-08-31 | Repository metadata for the **32 repos** this wiki writes entity pages for (7 tier 1 clients, 5 tier 2, 7 Orbit, 9 tools, 4 project repos), plus **24 READMEs** saved verbatim. One JSON file per repo (`<owner>__<name>.json`, `.release.json`) so a claim about a version, a licence or an archived flag can be re-checked against the exact response; `_index.md` is a generated table over them. Fetched because docs.nats.io states no client versions, licences or feature coverage and explicitly delegates coverage to "each repo's README". Re-fetch with `--refresh`. `nats-io__natscli.stream-backup-v0.4.0.md` (added 2026-08-31) holds `cli/stream_command.go`'s backup/restore flag definitions and the client-side rename check at natscli **v0.4.0**. `nats-io__k8s.values-nats-2.14.6.md` (added 2026-08-31) holds the quoted ranges of the Helm chart's `values.yaml` at chart release **nats-2.14.6** — lame-duck timing, `terminationGracePeriodSeconds`, the reloader sidecar — fetched from the tag rather than `main` so the numbers are pinned. `nats-io__natscli.account-tls-v0.4.0.md` (added 2026-08-31) holds `cli/account_tls_command.go` and `cli/account_command.go` at natscli **v0.4.0** — `nats account tls` (the certificate-chain check with `--expire-warn`, its non-zero exit and its deliberately stable grep pattern) and every `nats account backup` / `restore` flag. `nats-io__k8s.values-advertise-nats-2.14.6.md` (added 2026-08-31) is a second extract from the same `values.yaml` at chart release **nats-2.14.6**, taken for question-bank row 67: `config.cluster.noAdvertise: true` with its comment ("If clients are behind a load balancer it is best to leave this as is") and the `service:` block, which offers no `type` — so the chart's client Service is a plain ClusterIP and client discovery is **off** by default. Content is each project's own; Apache-2.0 except `nats-top` and `nats.ex` (MIT). Added 2026-09-03 (phase F step 1): `nats-io__nats.go.release-v1.48.0.md` — the v1.48.0 release body (2025-12-17) through `gh api repos/nats-io/nats.go/releases/tags/v1.48.0`, verbatim: the line "Add publish subject validation and a connection option to skip it (#1974, #1979)" that dates the docs' "nats.go before v1.48.0" claim. |
| cncf | `raw/cncf/` | https://www.cncf.io/projects/nats/ — fetched with `curl`, converted by `tools/html-to-text.py` (the `.html` original is kept next to the `.txt` extract) | 2026-08-31 | The foundation's project page for NATS. Read for one durable fact — "NATS was accepted to CNCF on March 15, 2018 at the **Incubating** maturity level" — which no source inside `nats-io` states. The page also embeds live LFX Insights metrics (health score, contributor counts); those move continuously and are only usable with the fetch date attached. Copyright The Linux Foundation / CNCF. |
| gh-issues | `raw/gh-issues/` | https://github.com/nats-io/nats-server/issues — fetched one issue at a time through the GitHub **GraphQL** API (`repository.issue(number:)`), which returns the body, every comment and the closing pull request as Markdown | 2026-08-31 | One file per issue, `issue-<number>.md`, with a provenance header and the state line naming the PR that closed it. Added 2026-08-31 for plan step 5: `issue-4281.md` (`insufficient storage resources available (10047)`, **open since 2023-06-29**, six reporters, and the maintainer statement that `max_bytes` is a reservation), `issue-8322.md` (the dynamic `max_file_store` shrinking at every restart, closed by PR #8503 merged 2026-08-24) and `issue-5871.md` (the same defect reported in 2024 and closed with "we do not recommend auto-sizing"). Separate from `gh-discussions` because issues carry a state, a closing PR and a fix version that discussions do not. Added 2026-09-02: `issue-5106.md` (object-store bucket mirrored from a leaf into a hub with another JetStream domain, `nats object ls` failing — closed 2024-03-04 after nats.go #1568/#1578; the header lists the cross-referenced client issues from the issue timeline) and `client-issues-object-store-mirror.md` (nats.go #1874, nats.go #1648, nats.js #155 — the three client-repository issues #5106 points at, fetched the same way; a different repository, kept here because they are the continuation of that issue). Added 2026-09-03: `issue-6921.md` (a `last_per_subject` consumer with explicit acks on a stream with `max_msgs_per_subject: 5` stops registering acks — floor frozen, redeliveries with acks that do not take — on 2.11.0–2.11.4, reproduced in .NET and Rust, bisected by a second reporter to fixed in 2.11.5; closed 2025-10-01 with no linked PR; cross-references #6795 and #6772 from the timeline). `exporter-issue-218.md` (added 2026-09-03, phase E step 3) is the one file here from another repository — `nats-io/prometheus-nats-exporter` issue #218, open and unanswered since 2023-04-11, where `nats_consumer_num_pending` differs per pod and the reporter settles on `is_consumer_leader="true"`; the header says so. Added 2026-09-03 (the `share` query): `issue-8271.md` — the `Nats-Request-Info` header pushing a service-import request over `max_payload`, **open**, `defect` + `stale`, with the whole thread of its fix PR #8278 (unmerged; two maintainers inclined not to enforce) rendered in the same file. |
| jwt-src | `raw/jwt-src/` | https://github.com/nats-io/jwt at tag **v2.8.2** — the version nats-server v2.14.6's `go.mod` pins — `v2/imports.go`, `v2/exports.go`, `v2/activation_claims.go` from `raw.githubusercontent.com` | 2026-09-03 | `imports-exports-activation-v2.8.2.md`: the quoted ranges with real line numbers — `Import.Share` / `Token`, `Export.TokenReq` / `Revocations` / `AccountTokenPosition`, the validators, and the activation claim. The library that defines the account JWT the server reads; added for the `share` query and `wiki/concepts/service-import-request-info.md`. |
| nsc-src | `raw/nsc-src/` | https://github.com/nats-io/nsc at tag **v2.15.0** (the latest release on 2026-09-03), `cmd/addimport.go`, `cmd/addexport.go`, `cmd/generateactivation.go` from `raw.githubusercontent.com` | 2026-09-03 | `import-export-activation-v2.15.0.md`: the flag registrations and the claim-building ranges — `add import --share` / `--token`, `add export --private` / `--account-token-position`, `generate activation --target-account`. `nsc` is not installed on this machine; the source is the authority for its flags. |
| gh-advisories | `raw/gh-advisories/` | https://github.com/advisories/GHSA-55h8-8g96-x4hj through the GitHub REST API (`/advisories/<ghsa>`), and the canonical NATS note https://advisories.nats.io/CVE/secnote-2026-08.txt with `curl` | 2026-09-03 | `GHSA-55h8-8g96-x4hj.md` (CVE-2026-33246, the leafnode spoofing of `Nats-Request-Info`, medium, fixed in 2.11.15 / 2.12.6) rendered from the API JSON with the description verbatim, and `secnote-2026-08.txt` as published. One file per advisory. Added for the `share` query: the only public statement of what the header is *for*. |
| nats-go-src | `raw/nats-go-src/` | https://github.com/nats-io/nats.go at tag **v1.53.1**, `nats.go` and `jetstream/errors.go` from `raw.githubusercontent.com` | 2026-08-31 | `errors-v1.53.1.md` holds the four error strings this wiki quotes with their real line numbers, so `nats: timeout` and `nats: no responders available for request` can be attributed rather than remembered. Apache-2.0. Only the quoted lines are stored; per-client API documentation is out of scope (`CLAUDE.md`). Added 2026-09-02: `kv-object-mirror-v1.53.1.md` — `jetstream/kv.go` (the subject templates, the mirror branch of `CreateKeyValue`, and `mapStreamToKVS` where the read prefix is rewritten to the origin only for an `External` mirror) and `jetstream/object.go` (`ObjectStore` binding the bucket by stream name), read to explain why a same-domain KV mirror is not readable by its own name and an object-store mirror needs a transform. **2026-09-04 (phase F step 3)**: `connection-v1.53.1.md` — 26 verbatim ranges of `nats.go` at v1.53.1: the default const block, `GetDefaultOptions`, the seven `Status` values, `defaultErrHandler` and where it is installed, `selectNextServer`, `doReconnect`, the reconnect-buffer check, `processAuthError`, `processPingTimer`, `drainConnection`, `Drain`, `Flush`, `ForceReconnect`, `StatusChanged` and the per-subscription pending defaults. **2026-09-04 (phase F step 4)**: `subscription-v1.53.1.md` — 21 further ranges of the same file: the pending-limit defaults and where they are applied at subscribe time, `PendingLimits` / `SetPendingLimits` / `Pending` / `Dropped`, the overflow path with its one-shot slow-consumer transition, `NextMsg`'s validity check, `processTransientError`, `processAuthError`, `checkAuthError`, `processErr`'s three fates, `UserCredentials` and `userFromFile`, `connectProto`, and `Request`'s 503 handling. |
| gh-discussions-index | `raw/gh-discussions-index/` | https://github.com/nats-io/nats-server/discussions — the whole list through the GitHub **GraphQL** API (`repository.discussions(first:100, orderBy:CREATED_AT)`, five pages), fetched by `tools/triage-discussions.py` | 2026-09-03 | `discussions-<DATE>-p<n>.json` are the API replies verbatim (number, title, URL, category, author, opened, updated, chosen answer and by whom, upvotes, comment count, closed, labels, and the **original post's body**; no comments or replies); `discussions-<DATE>.md` renders them one `## gh#<n> — <title>` section per thread, so the viewer serves it and `inbox/gh-discussions-toc.md` anchors into it. 484 threads on 2026-09-03 (Q&A 351, General 99, Ideas 33, Show and tell 1). A thread that matters is fetched whole into `raw/gh-discussions/` with `tools/fetch-discussion.py`; this index is for triage and search, and a re-run (quarterly, per the maintenance triggers) adds a new dated set rather than touching this one. Content is the authors' own, public on GitHub. |
| prometheus-nats-exporter-src | `raw/prometheus-nats-exporter-src/` | https://github.com/nats-io/prometheus-nats-exporter at tag **v0.20.2** — `collector/*.go`, `exporter/exporter.go`, `main.go` from `raw.githubusercontent.com`; and the binary from `go install github.com/nats-io/prometheus-nats-exporter@v0.20.2` (module version confirmed with `go version -m`) | 2026-09-03 | `collector-v0.20.2.md` holds the verbatim line ranges behind `wiki/reference/metrics.md` — the two namespaces (`gnatsd`, `jetstream`), the generic flattening rule, every hand-written series with its labels, the `/jsz` query each `-jsz` value sends, the inverted "defaulting to varz" test in `main.go`. `metrics-observed-v0.20.2.md` is ten scrapes of the exporter against the v2.14.6 lab cluster (every collector on; `-prefix nats`; `-connz_detailed`; `-jsz=streams`; no flags; `-jsz=all` alone; `-use_internal_server_name`; the stream leader's node; a live client) with the Go runtime series dropped and the count stated; `metrics-run.sh` and `metrics-run2.sh` beside it are the scripts. Apache-2.0. Captured output, not a fetched document — regenerate with the scripts on `tools/lab/cluster.sh up 3`. |
| nats-surveyor-src | `raw/nats-surveyor-src/` | the `nats-surveyor` binary from `go install github.com/nats-io/nats-surveyor@v0.9.11` (module version confirmed with `go version -m`), run against the lab; the appendix quotes the v0.9.11 source from the Go module cache (`cmd/root.go`, `surveyor/surveyor.go`, `surveyor/collector_statz.go`) | 2026-09-03 | `metrics-observed-v0.9.11.md`: three scrapes over the system account of the v2.14.6 lab cluster (`--jsz all --accounts --raftz`; the same with `--jsz-leaders-only`; `--prefix x`), Go runtime series dropped with the count stated, plus the source lines that build every name with the literal `nats` namespace and the `Prefix string // TODO` field that makes `--prefix` a no-op. Apache-2.0. The script is `raw/prometheus-nats-exporter-src/metrics-run.sh`. |
