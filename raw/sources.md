# Raw sources manifest

Immutable originals live in subfolders of `raw/`, one folder per collection. Never edit them
after creation; derived copies (UTF-8 conversions, PDF text extracts) go next to the original
with a suffix (`-utf8.txt`, `.txt`). One row per collection; the viewer shows this table on the
Sources page.

| collection | path | origin | fetched | notes |
|---|---|---|---|---|
| adr | `raw/adr/` | https://github.com/nats-io/nats-architecture-and-design (tarball of `main`) | 2026-08-31 | 54 ADRs plus the repo index (`_repo-index.md`) and the ADR template. Apache-2.0. Triaged into `inbox/adr-toc.md` by `tools/triage-adrs.py`. |
| nats-docs | `raw/nats-docs/` | https://docs.nats.io — paths taken from the site's `llms.txt`, fetched by `tools/fetch-docs.py` | 2026-08-31 | 861 pages of the docs in their Markdown form, mirroring the site tree (`concepts/`, `learn/`, `reference/`, `tutorials/`, `release-notes/`). `reference/config/` (621 pages) is generated from the server, not hand-written — `tools/build-config-table.py` turns it into `inbox/config-keys-table.md`. Docs are versioned; this is the 2.14 (latest) tree. Each file carries a one-line provenance header. Copyright Synadia Communications, Inc. |
| synadia-blog | `raw/synadia-blog/` | https://www.synadia.com/blog/ — individual posts, fetched with `curl` and converted by `tools/html-to-text.py` (the `.html` original is kept next to the `.txt` extract) | 2026-08-31 | Public Synadia engineering blog. Posts: `nats-jetstream-high-ram-usage` (Andrew Connolly, 2025-08-08, series *NATS Community FAQs*) and `jetstream-design-patterns-for-scale` (Andrew Connolly, 2026-06-06, titled *JetStream Anti-Patterns*, series *Design Patterns for Scaling NATS*). Site navigation and footer survive at the top and bottom of the extract — that is intended, the extract is verbatim. Copyright Synadia Communications, Inc.; summarize and paraphrase only. |
| release-notes | `raw/release-notes/` | https://github.com/nats-io/nats-server/releases via the GitHub API (`releases/tags/<tag>` and `releases?per_page=100`) | 2026-08-31 | `v2.14.0.md` is the release body verbatim. `_tags-and-dates.md` is the full tag list — **291 tags** with publish date and prerelease flag — so every version and date this wiki states is traceable to one file. Confirms there is **no 2.13**. Apache-2.0. |
| gh-discussions | `raw/gh-discussions/` | https://github.com/nats-io/nats-server/discussions — fetched one thread at a time through the GitHub **GraphQL** API (`repository.discussion(number:)`), which returns the original post, the chosen answer, every comment and every reply as Markdown | 2026-08-31 | One file per thread, `gh-<number>.md`, with a provenance header and the category, open date, author and whether an answer was chosen. GraphQL is used in preference to scraping the HTML: the bodies come back as the authors wrote them, so a claim can be quoted exactly. Public threads only. |
| nats-server-src | `raw/nats-server-src/` | https://github.com/nats-io/nats-server at tag **v2.14.6**, individual `server/*.go` files from `raw.githubusercontent.com` | 2026-08-31 | `constants-v2.14.6.md` holds the exact line ranges this wiki quotes defaults from — `const.go`, `consumer.go`, `stream.go`, `filestore.go`, `server.go` — verbatim, with their real line numbers, so each value links to `…/blob/v2.14.6/server/<file>#L<line>`. Only the quoted ranges are stored, not whole files. `README-v2.14.6.md` is the repository README at the same tag, added 2026-08-31 — the only public source read for CNCF membership, the licence statement and the Trail of Bits / OSTIF security audit. Apache-2.0. |
| github-repos | `raw/github-repos/` | https://api.github.com/repos/{owner}/{name} and `.../releases/latest` and `.../readme`, through the `gh` CLI — fetched by `tools/fetch-repo-facts.py` | 2026-08-31 | Repository metadata for the **32 repos** this wiki writes entity pages for (7 tier 1 clients, 5 tier 2, 7 Orbit, 9 tools, 4 project repos), plus **24 READMEs** saved verbatim. One JSON file per repo (`<owner>__<name>.json`, `.release.json`) so a claim about a version, a licence or an archived flag can be re-checked against the exact response; `_index.md` is a generated table over them. Fetched because docs.nats.io states no client versions, licences or feature coverage and explicitly delegates coverage to "each repo's README". Re-fetch with `--refresh`. Content is each project's own; Apache-2.0 except `nats-top` and `nats.ex` (MIT). |
| cncf | `raw/cncf/` | https://www.cncf.io/projects/nats/ — fetched with `curl`, converted by `tools/html-to-text.py` (the `.html` original is kept next to the `.txt` extract) | 2026-08-31 | The foundation's project page for NATS. Read for one durable fact — "NATS was accepted to CNCF on March 15, 2018 at the **Incubating** maturity level" — which no source inside `nats-io` states. The page also embeds live LFX Insights metrics (health score, contributor counts); those move continuously and are only usable with the fetch date attached. Copyright The Linux Foundation / CNCF. |
