<!-- source: https://docs.nats.io/reference/config/gateway/listen.md · fetched 2026-08-31 · section: listen -->
# listen

Requires Restart

`<host>:<port>` format. Alternative to `host`/`port`.

## Types

| Type     | Description                                                       | Choices |
| -------- | ----------------------------------------------------------------- | ------- |
| `string` | This is an alternate to setting the `host` and `port` separately. | -       |
