<!-- source: https://docs.nats.io/reference/config/resolver/dir.md · fetched 2026-08-31 · section: dir -->
# dir

Hot Reloadable

Handled only as part of the whole `resolver` block; replacement DirAccResolver is never Start()ed.

The path to storing account JWT files when pushed to the server.

## Types

| Type     | Description | Choices |
| -------- | ----------- | ------- |
| `string` | -           | -       |
