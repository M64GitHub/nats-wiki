<!-- source: https://docs.nats.io/reference/config/logfile.md · fetched 2026-08-31 · section: logfile -->
# logfile

Aliases:

<!-- -->

`log_file`

Hot Reloadable

Ignored if `-l` or `--log` was passed at startup — the flag value is re-applied over the config on every reload.

Log file name.

## Types

| Type     | Description | Choices |
| -------- | ----------- | ------- |
| `string` | -           | -       |
