<!-- source: https://docs.nats.io/reference/config/http.md · fetched 2026-08-31 · section: http -->
# http

Requires Restart

Listen specification `<host>:<port>` for server monitoring.

## Types

| Type     | Description                                                       | Choices |
| -------- | ----------------------------------------------------------------- | ------- |
| `string` | This is an alternate to setting the `host` and `port` separately. | -       |
