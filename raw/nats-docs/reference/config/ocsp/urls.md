<!-- source: https://docs.nats.io/reference/config/ocsp/urls.md · fetched 2026-08-31 · section: urls -->
# urls

Hot Reloadable

`url` and `urls` write the same field; setting both means last-parsed wins.

An explicit list of override URLs to request staples.

## Types

| Type         | Description | Choices |
| ------------ | ----------- | ------- |
| `[ string ]` | -           | -       |
