<!-- source: https://docs.nats.io/reference/config/logtime.md · fetched 2026-08-31 · section: logtime -->
# logtime

Hot Reloadable

Ignored if `-T` or `-logtime` was passed at startup — command-line boolean flags are re-applied over the config on every reload.

If false, log without timestamps.

## Types

| Type     | Description | Choices |
| -------- | ----------- | ------- |
| `string` | -           | -       |
