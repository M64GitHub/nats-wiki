<!-- source: https://docs.nats.io/reference/config/mqtt/authorization/token.md · fetched 2026-08-31 · section: token -->
# token

Requires Restart

Specifies a global token that clients can use to authenticate with the server (exclusive of `user` and `password`).

## Types

| Type     | Description | Choices |
| -------- | ----------- | ------- |
| `string` | -           | -       |
