<!-- source: https://docs.nats.io/reference/config/mqtt/authorization/username.md · fetched 2026-08-31 · section: username -->
# username

Aliases:

<!-- -->

`user`

Requires Restart

Specifies a global user name that clients can use to authenticate the server (requires `password`, exclusive of `token`).

## Types

| Type     | Description | Choices |
| -------- | ----------- | ------- |
| `string` | -           | -       |
