<!-- source: https://docs.nats.io/reference/config/http_base_path.md · fetched 2026-08-31 · section: http_base_path -->
# http\_base\_path

Requires Restart

If `--http_base_path` was passed at startup the flag value is re-applied over the config on every reload, so the edit is discarded rather than rejected.

Base path for monitoring endpoints.

## Types

| Type     | Description | Choices |
| -------- | ----------- | ------- |
| `string` | -           | -       |
