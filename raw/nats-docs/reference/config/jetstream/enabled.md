<!-- source: https://docs.nats.io/reference/config/jetstream/enabled.md · fetched 2026-08-31 · section: enabled -->
# enabled

Aliases:

<!-- -->

`enable`

Hot Reloadable

Also covers the boolean and "enabled"/"disabled" string forms of the top-level jetstream key (opts.go:2644-2654).

If true, enables the JetStream subsystem.

## Types

| Type      | Description | Choices         |
| --------- | ----------- | --------------- |
| `boolean` | -           | `true`, `false` |
